# Copy to .env.local (or pass inline) to require a passcode to open the quizmaster
# console. When unset, no passcode is needed — fine for local testing.
#
#   HOST_PASSCODE=choose-something
#
# Inline for one run:  HOST_PASSCODE=swordfish npm start
HOST_PASSCODE=
